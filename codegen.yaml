schema: ./src/schema/index.ts
generates:
  ./src/interfaces/resolvers-types.ts:
    config:
      useIndexSignature: true
      contextType: ./#Context
    plugins:
      - typescript
      - typescript-resolvers